# Optional. When set, place search uses Google Places (Text Search, New) instead of
# the free OpenStreetMap/Nominatim search. Restrict the key to your site's HTTP referrers.
VITE_GOOGLE_MAPS_API_KEY=
